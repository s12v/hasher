{
  "name": "hasher",
  "version": "1.5.0",
  "private": true,
  "description": "Developer's toolbox browser extension: hashes, HMAC, CRC, ciphers, IP/subnet, time, number and string conversions",
  "license": "MIT",
  "scripts": {
    "test": "node --test \"test/*.test.js\"",
    "build": "node scripts/build.js"
  },
  "engines": {
    "node": ">=20"
  }
}
